const ScrollProgress = () => {
    return (
        <div className="scroll-progress hidden md:block" id="scrollProgress">
            <div className="scroll-progress-bar" />
        </div>
    );
};

export default ScrollProgress;
